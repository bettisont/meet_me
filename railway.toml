[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
serviceName = "backend"
source = "backend"

[services.variables]
NODE_ENV = "production"